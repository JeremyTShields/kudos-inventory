# Server Environment Variables
# Copy this file to .env and update with your values

# Server Configuration
NODE_ENV=development
PORT=4000

# CORS Configuration (comma-separated origins)
CORS_ORIGIN=http://localhost:3000,http://localhost:5173,http://localhost:5174,http://localhost:5175

# Database Configuration
# DB_DIALECT defaults to mysql. Set DB_DIALECT=sqlite for a zero-install
# local database (DB_STORAGE sets the file path, default dev.sqlite).
DB_DIALECT=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=kudos
DB_USER=root
DB_PASS=your-database-password

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES=24h
JWT_REFRESH_EXPIRY=7d
